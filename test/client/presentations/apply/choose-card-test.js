'use strict';

import assert         from 'assert';

import 'jsdom-global/register';
import React          from 'react';
import { render }     from 'enzyme';
import sinon          from 'sinon';

import ChooseCard     from '../../../../client/containers/apply/what-do-you-want-to-do-today-container.jsx';

describe('Choose Card Form', function() {
  // next button disabled on render
  // checkboxes with values DL and ID

});
